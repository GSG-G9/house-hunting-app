import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  header: {
    marginTop: 60,
  },
  container: {
    marginTop: 60,
    minHeight: 500,
  },
  search: {
    marginTop: 20,
  },
}));

export default useStyles;
