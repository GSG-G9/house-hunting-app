import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    background: '#FFFFFF',
    width: '85%',
    padding: '3em',
    height: '100%',
  },
}));

export default useStyles;
